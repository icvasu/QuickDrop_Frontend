/** @type {import('next').NextConfig} */
const nextConfig = {
  // Disable the Next.js development indicator (the "N" button)
  devIndicators: {
    buildActivity: false,
  },
  reactStrictMode: true,
};

export default nextConfig;